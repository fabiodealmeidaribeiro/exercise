const { name, lastname, height, age } = {
    name : 'Fábio',
    lastname : 'Ribeiro',
    height : 1.73,
    age : 38,
};

console.log(name);

for (let index = 0; index < 3; index++) console.log('---/---');

const [ x, y, z ] = [ 'apple', 'orange', 'grape' ];

console.log(y);